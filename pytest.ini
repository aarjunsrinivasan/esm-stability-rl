[pytest]
testpaths = tests
# `.` lets tests do `from align import train_dpo`; `align` additionally satisfies the
# sibling imports inside those scripts (`from train_dpo import seq_logp`), which
# otherwise only resolve when a script is run directly as `python align/scoring.py`.
pythonpath = . align
addopts = -q --strict-markers
